/** Type declaration for the pdf-parse library (sub-path import used in bundled builds). */
declare module "pdf-parse/lib/pdf-parse.js" {
  import pdfParse = require("pdf-parse");
  export = pdfParse;
}
